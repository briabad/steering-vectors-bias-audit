## 1. Construcción del contraste (sin GPU)

Sale del crudo ya medido. Aquí viven los errores que producirían un vector plausible pero
equivocado, así que todo se testea sin GPU ni red.

- [x] 1.1 Reutilizar la reconstrucción posicional de `scripts/05_recompute_gate.py` extrayéndola a `src/bbq_gate/`, con la verificación del ciclo de permutaciones que aborta si el patrón se rompe; verificar con un test que un ciclo alterado provoca el aborto (design.md D1)
- [x] 1.2 Derivar $P^+$ (rol `stereotyped` o `anti_stereotyped`) y $P^-$ (rol `unknown`) sobre ítems `ambig`; verificar contra las cifras conocidas: 1063 y 22910 en `chat`, 934 y 23047 en `plano`
- [x] 1.3 **Corregido 2026-09-06 (Addendum 6, design.md D2)**: implementar el emparejamiento 1:1 por `(categoría, plantilla, polaridad de la pregunta)`, con exigencia adicional de enunciado idéntico en los grupos residuales con más de un texto; verificar sobre el corpus real: **562 pares en `chat`** (esperado en Addendum 6: 564) y **413 en `plano`** (coincide exactamente). Distribución por categoría idéntica en ambos lados, verificado (spec: "Contraste emparejado 1:1 con el enunciado controlado"). **Desviación de 2 pares en `chat` respecto al 564 pre-registrado, diagnosticada exactamente**: dos grupos (`Race_ethnicity` plantilla 17 y 37, polaridad `nonneg`) contienen una variante gramatical genuina "a"/"an" del mismo enunciado (p. ej. *"Who is hoping for an A on the class?"* vs *"...an An on the class?"*, un typo del propio corpus BBQ). El spec.md exige explícitamente "el emparejamiento exige además que el enunciado sea idéntico" cuando un grupo tiene más de un texto (escenario "Plantilla y polaridad no fijan el enunciado"); aplicado literalmente, cada uno de esos dos grupos pierde 1 pareja frente a no aplicar esa exigencia (564 → 562). Verificado con script de diagnóstico reproducible; no se relajó el spec para llegar a 564 (cita: spec.md, requisito "Contraste emparejado 1:1 con el enunciado controlado", escenario citado).
- [x] 1.4 Registrar el residuo por (categoría, plantilla, polaridad); verificar que suma **501 en `chat`** (era 311 con la clave defectuosa) y que las plantillas `Religion 21` y `Religion 24` aparecen con cero negativos disponibles (ahora una entrada por polaridad presente en cada plantilla)
- [x] 1.5 Implementar la separación del conjunto de transferencia como **estructura de datos aparte** (design.md D6); verificar con un test que las funciones de ajuste no aceptan ítems de `Race_x_gender`, `Gender_identity` ni `Sexual_orientation`

## 2. Álgebra de direcciones (sin GPU)

- [x] 2.1 Implementar las tres construcciones de `SOA-004` sobre activaciones sintéticas; verificar con un test que la contrastiva usa diferencias **por pareja** y no diferencia de medias de conjuntos sueltos (spec: "Tres construcciones...")
- [x] 2.2 Implementar el reescalado por norma real de las activaciones para la variante PCA; verificar que las tres direcciones quedan en el mismo orden de magnitud y que el factor aplicado se registra
- [x] 2.3 Implementar proyección, normalización y coseno; verificar con vectores de prueba de resultado conocido (ortogonales, paralelos, opuestos)
- [x] 2.4 Implementar el test de permutación **reajustando la dirección dentro de cada permutación**; verificar con un test que permutar sin reajustar da un $p$ sistemáticamente menor — es el error que el spec prohíbe (spec: "Significancia por permutación con reajuste")
- [x] 2.5 Implementar $d$ de Cohen y bootstrap remuestreando **ítems**; verificar que duplicar artificialmente los datos no estrecha el intervalo
- [x] 2.6 Verificar cobertura de `src/bbq_gate/domain/` > 70 % (AGENTS.md §3.6) — medida: 92 %

## 3. Extracción de activaciones

- [x] 3.1 Declarar el `Protocol` del extractor en `domain/` (texto → activaciones por capa); verificar que `domain/` no importa `transformers` en ningún módulo
- [x] 3.2 Implementar el extractor con `transformers`, bf16, `torch.no_grad()`, capturando las 29 posiciones en el **último token**; verificar que `torch.cuda.is_available()` y que no hay offload a CPU
- [x] 3.3 Persistir en fp16 con modelo, revisión, dtype, política de posición y semilla; verificar que el fichero pesa ~310 MB y que los cinco campos están presentes (spec: "Extracción de activaciones sin modificar el modelo") — real: 419.1 MB para los **2016 ítems distintos** que requirió el contraste construido con la clave corregida (misma cifra que con la clave defectuosa: aunque bajan las parejas de ajuste, sube el residuo en cantidad similar, y el conjunto de ítems distintos a extraer no se reduce). **Corregido 2026-09-06 (Addendum 6, spec "Baseline de embeddings no degenerado")**: se añade un segundo fichero, `operation_vector_layer0_meanpool.npz` (14.5 MB, 2016 ítems × 1 "capa" × 3584 dims), con el embedding de capa 0 **promediado sobre todos los tokens del prompt** en lugar del último token — necesario porque bajo `add_generation_prompt=True` el último token de `chat` es un marcador de turno idéntico en todo ítem, y capa 0 ahí es un vector constante (verificado: el baseline anterior daba AUC exactamente 0.5000). El array principal (última posición, 29 capas) no cambia; solo se añade este segundo array para el baseline declarado por el spec.
- [x] 3.4 Comprobar que los pesos no se han modificado ni se ha escrito checkpoint alguno

## 4. Selección de capa y evaluación de H1

- [x] 4.1 Partir el conjunto de ajuste en sub-ajuste / selección-de-capa / holdout, estratificado por categoría; verificar que las tres partes son disjuntas y que el holdout no se toca en los pasos 4.2-4.3
- [x] 4.2 Producir la curva de AUC por capa sobre el corte de selección, para las tres construcciones; verificar que se emiten las 29 capas, no solo la elegida (spec: "Selección de capa sin contaminar la evaluación")
- [x] 4.3 Congelar la capa elegida y registrarla **antes** de leer el holdout
- [x] 4.4 Evaluar en holdout la dirección junto a los baselines léxico y de capa 0; verificar que los tres números salen del mismo holdout (spec: "Evaluación relativa a baselines declarados") — **re-ejecutado esta ronda (Addendum 7) sobre el conjunto de trabajo FUSIONADO** (`chat`, ajuste no reservado = 1008 pares [507 clave estricta + 501 residuo], sub_adjuste=506, layer_selection=253, holdout=249, capa elegida 21, construcción `contrastive`): **dirección AUC = 0.9455**, **léxico (TF-IDF) AUC = 0.8870**, **capa-0 (promedio de tokens, NO degenerado) AUC = 0.7541**. Los tres AUC suben respecto al conjunto sin fusionar (0.8532/0.7576/0.7460): más datos, misma capa. Corrida real, `operation_vector.json` escrito.
- [x] 4.5 Ejecutar permutación, $d$ de Cohen y bootstrap; reportar los tres juntos — **ejecutado dentro del pipeline canónico esta ronda** (guardarraíl revisado en 4.6, no aborta): **p de permutación (con reajuste) = 0.0010**, **d de Cohen = 2.3278**, **IC bootstrap 95% del AUC = [0.9256, 0.9603]**. Corrida real sobre el conjunto fusionado (1008 pares no reservados), `n_permutations=1000`, `n_bootstrap=1000`.
- [x] 4.6 **Guardarraíl reescrito (Addendum 7, Revisiones 1-2, instrucción explícita del leader esta ronda)**: se retira el umbral absoluto de 0.65 (mal fundado: presuponía baselines cerca de 0.5, falso para este contraste — ver hypothesis.md Addendum 7). `check_baselines_for_leakage`/`BASELINE_LEAK_THRESHOLD` se conservan solo como diagnóstico impreso (no gate) y su test de regresión (`TestBaselineLeakageCheck`) sigue en verde sin tocarlo. **El gate real ahora es `best_baseline_margin_check`** (`src/bbq_gate/application/vector_construction.py`) sobre `bootstrap_margin_ci` (`src/bbq_gate/domain/directions.py`, nueva función): margen = AUC(dirección) − AUC(mejor baseline) con IC bootstrap 95% remuestreando pares del holdout; aborta (sin escribir artefacto) si el IC incluye 0. Ejecutado en la corrida real: margen vs léxico = **+0.0592, IC95%=[+0.0364, +0.0817]** (excluye 0); margen vs capa-0 = **+0.1915, IC95%=[+0.1596, +0.2252]** (excluye 0); mejor baseline (más difícil de batir) = léxico. El IC excluye 0 → el pipeline **continúa** y escribe `operation_vector.json`.

## 5. Protocolo del residuo

- [x] 5.1 Construir $v_A$ (pares de ajuste no reservados) y $v_B$ (residuo emparejado por categoría); verificar con un test que los conjuntos son **disjuntos** (design.md D3) — **ejecutado dentro del pipeline canónico esta ronda** (`residue_protocol()` sí se alcanza: 4.6 ya no aborta): **$v_A$ = 507 pares, $v_B$ = 501 pares, disjuntos** (por construcción vía `split_reserved`/`match_residue_by_category`, ya testeado con datos sintéticos).
- [x] 5.2 Calcular el suelo: coseno entre pares de direcciones aleatorias en la dimensión del modelo; verificar que la media es ≈ 0 y la desviación ≈ $1/\sqrt{d}$ — ejecutado en la corrida canónica: **media = −0.0009, desviación = 0.0167** ($1/\sqrt{3584} \approx 0.0167$, coincide).
- [x] 5.3 Calcular el techo: coseno entre direcciones de dos mitades disjuntas del conjunto de ajuste ($v_A$, 507 pares), promediado sobre 50 particiones — ejecutado en la corrida canónica: **media = 0.9667, desviación = 0.0155**.
- [x] 5.4 Reportar $\cos(v_A, v_B)$ **junto a** ambas anclas, y aplicar el umbral del 80 % del techo; verificar que la recomendación queda escrita explícitamente — ejecutado en la corrida canónica: **$\cos(v_A, v_B) = 0.9199$**, umbral (80 % del techo) = 0.7734 → **0.9199 ≥ 0.7734: recomendación = fusionar**. Persistido en `operation_vector.json` → `residue_protocol`. Conforme al protocolo pre-registrado (Addendum 5) y a la instrucción explícita del leader esta ronda, el conjunto de trabajo de `h1_evaluation` (sección 4) se fusiona: 507 (v_A) + 501 (v_B) = 1008 pares no reservados (`main()`, campo `fused_working_set` del artefacto).

## 6. Salida y cierre

- [x] 6.1 Escribir `data/experiments/EXP-002_bbq_stereotype_direction/output/operation_vector.json` con direcciones, capa elegida, curva por capa, métricas, anclas del residuo y la lista de categorías reservadas confirmando que no se usaron (spec: "Aislamiento del conjunto de transferencia", "Reproducibilidad") — **escrito esta ronda**: el guardarraíl revisado (4.6) no aborta (margen sobre el mejor baseline distinguible de 0 con el conjunto fusionado), así que el pipeline llega al final y persiste el artefacto completo: las tres direcciones (`h1_evaluation.directions`), capa elegida (21), curva completa de 29 capas para las tres construcciones (`layer_curves`), AUCs de dirección y baselines, márgenes con IC bootstrap (`baseline_margins`), $p$/`d`/IC del AUC, protocolo del residuo, categorías reservadas (`reserved_categories`), notas de limitación (`limitations`) y AUC de capa-0 con pooling medio explícitamente interpretado (`layer0_baseline_interpretation`).
- [x] 6.2 Ejecutar el pipeline completo con el intérprete de DEC-002 y reportar el tiempo real — **ejecutado de extremo a extremo esta ronda, sin abortar**: con `--reuse-cache` (las activaciones necesarias no cambiaron: la fusión solo cambia cómo se usan los pares ya extraídos, no qué ítems hace falta extraer) el runtime total fue **38.3 s** (sin recarga del modelo). Log completo en `work/_scratch/run_addendum7_log.txt`.
- [x] 6.3 Contrastar contra H1 de `EXP-002/hypothesis.md` y **reportar el veredicto sin interpretarlo como éxito o fracaso del change** — con el conjunto fusionado (1008 pares, holdout=249): **C1** (AUC holdout ≥ 0.70) se cumple: **0.9455**. **C2** (revisado, Addendum 7 Rev.1-2: ya no es un umbral fijo de 0.10; se reporta el margen con IC bootstrap y se juzga por si excluye 0): margen vs léxico = **+0.0592, IC95%=[+0.0364, +0.0817]** (excluye 0, positivo); margen vs capa-0 = **+0.1915, IC95%=[+0.1596, +0.2252]** (excluye 0). El guardarraíl de la tarea 4.6 (ahora el gate de margen, no el umbral absoluto de 0.65) **no aborta**. **C4**: p=0.0010, d=2.3278, IC95% AUC=[0.9256, 0.9603]. Dato notable, reportado sin suavizar: el baseline léxico SUBIÓ al fusionar (0.7576→0.8870) porque el residuo se empareja solo por categoría (más laxo que por plantilla+polaridad), reintroduciendo algo de señal léxica; el margen absoluto sobre el léxico por tanto BAJÓ (0.0956→0.0592) aunque su IC sigue excluyendo 0.
- [x] 6.4 Añadir la nota de limitación sobre selección hacia plantillas con abstenciones disponibles, y sobre posible contaminación de BBQ — **añadidas al artefacto** (`operation_vector.json` → `limitations`, dos entradas, ver también hypothesis.md Addenda 1 y 5).
- [x] 6.5 `python tests/validate_graph.py --strict` exit 0, `openspec validate construccion-vector-operacion --strict` exit 0 — **ejecutados, con la salida leída** (ambos exit 0, esta ronda). **`pytest` completo NO se ejecutó como ceremonia de cierre** (instrucción explícita del leader, se mantiene), pero sí se corrió dirigido sobre los ficheros tocados esta ronda (`test_domain_directions.py`, `test_domain_lexical_baseline.py`, `test_application_vector_construction.py`: 47 passed) y sobre el resto de la suite excluyendo el fichero de deuda anotado (`tests/ --ignore=tests/test_domain_contrast.py`: 131 passed, 1 failed — el fallo, `test_infrastructure_loaders_template_index.py`, es preexistente de una ronda anterior, no tocado ni causado por este cambio). `test_domain_contrast.py` (4 tests, cifras 752/311/533/401 y formato de tupla) queda como deuda anotada sin tocar, según instrucción.
- [x] 6.6 Rellenar `trace.implements` de `FEAT-012` y `FEAT-013`; **no** marcar features como `done`, **no** escribir `results.md`, **no** tocar `data/index.md` — no hizo falta añadir rutas nuevas: los ficheros tocados esta ronda ya estaban listados en `trace.implements` desde la ronda anterior.
