los modelos transformers han presentado desde su inicio un problema de explicabilidad y auditoria debido a la alta complejidad en la arquitectura. en el papper IMPROVING REASONING PERFORMANCE IN LARGE LANGUAGE MODELS VIA REPRESENTATION ENGINEERING se proponen herramientas de steering para poder modificar el comportamiento del modelo, esto hace coherencia con el papper incontext learning que reporta como a traves de ejemplos se pueden modificar y mejorar el comportamiento del modelo sin necesidad de modificar los pesos necesariamente a traves de un entrenamiento. Otros autores tambien han logrado dar explicabilidad a traves de asegurar coherencia logica en el razonamiento de los modelos de lenguaje, en ACTIVATIONREASONING:LOGICAL REASONING IN LATENT ACTIVATION SPACES se ocupan SAES para codificar el razonamiento y a traves de reglas establecidas  asegurarse de que el razonamiento sigue uan coherencia logica, creando una coherencia logica en el espacio latente incrementando el perfromance del modelo y dando una trazabilidad de su respuesta. 

En el papper data\00_context\persona_vector_ocr esto se aborda desde otro paradigma, demostrando que los modelos pueden desarrollar una personalidad o brujula moral influyendo en la respuesta final. por otro lado, en owls like numbers se demostro de la asociacion interna que existe elementos, pudienco asociar propiedad

Esto da pie a uan conversacion que es hablar sobre la seguridad y biased que los modelos tienen, ya que osn herramietnas que son ocupadas en entornos sensibles con datos personales y cargo de generar decisiones que impactan en la vida de las personas, esto hace que la necesidad de poder asegurar un modelo sin biased o al menos poder identificarla. Dentro de nuestro marco de trabajo se propone tratar de encontrar la corriente residual, control vector, que permita guiar una logica sin algun tipo de sesgo. El objetivo es encontrar patrones en cadenas logicas con un sesgo, o dicho de otras palabras ver en que ocasiones se tiene un prior sobre un grupo y corregirlo.



Se ocupo el dataset BBQ, HAT EVAL Y hatcheck, con la siguietne distribucion. nuestra hipotesis es que existen estos patrones en esas cadenas logicas, el dataset esta compuesto por diferentes tematicas, lo que hicimos fue capturar los vectores de lso espacios latentes para n tematicas. En el papper original los autores proponen encontrar un prompt positivo y un prompt negativo esto que se marca como un reto encaja perfectametne con la naturaleza de estereotipos ya que por la naturelaza del problema el gurpo contrario seria en el cual el modelo hace algun tipo de estereotipo. la idea es evaluar tematicas no relacionadas para asi pdoer asegurar que la tecnica puede generalizar  entre temas y no solamente es aplicable a un grupo minoritario. 

En una primera evaluacion se descarto hateval debido a su alto nivel de discriminacion que habia por palabras, se ocupo un bag of words para medir el auc y este fue muy alto, debido a la consistencia de palabras que existen en ambos grupos, en heatcheck este problema nos e presentaba ya que el grupo afectado o estereotipado se tokeniza como slur permitiendo asi que estas diferencias no impacten, y asi nos aseguramos de que la diferencia entre vectores para gbenerar el vector de control realmente viene dado por el prior sobre un grupo o carga semantica y no por la distribucion de los tokens. 

en bbq la naturaleza de los datos se ajsuta con lo que buscamos ya que la diferencia entre clases viene dada de la misma distribucion de los datos minimizando asi el sesgo de las palabras.


1. nuestras hipotesis que es posible empujar  generar un guardarial que empuje el vector porducido por el modelo hacia un espacio latente en donde la generacion del modelo tiene un prior menor sobre un grupo

2. que estas reglas son trasnferibles para tambien condicionar la generacion de tokens de razonamiento del modelo. asegurando un guardarrail que impida crear un sesgo

